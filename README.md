# Self in Layers — Project Stub

This repository contains the **scaffold** for your final project website.

## Pages
- `index.html` — Home
- `archive.html` — Anatomy Archive (body-region index)
- `torso.html`, `skull.html`, `hands.html`, `arms.html`, `back.html` — Region pages
- `object.html` — Repeating content template (single study)
- `cabinet.html` — Cabinet of Anatomy (fragments)
- `timeline.html` — Timeline of Study
- `about.html` — About the project
- `works-cited.html` — Works cited

## Styles
- `styles/style.css` — Starter stylesheet (you may replace or empty it if required).

## Suggested Git commands
```bash
git init
git add .
git commit -m "Initial stub: Self in Layers site"
git branch -M main
# Create a new GitHub repo first, then:
git remote add origin https://github.com/<your-username>/self-in-layers.git
git push -u origin main
```
